<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.finalproject.mapper.MyRouteMapper">

    <select id="findAll" resultType="MyRoute">
        select * from myroute
    </select>

    <insert id="save" parameterType="MyRoute">
        insert into myroute values(seq_myroute.nextval, #{route_id}, #{member_id})
    </insert>

    <update id="update" parameterType="MyRoute">
        update myroute set
          route_id=#{route_id},
          member_id=#{member_id}
        where id=#{id}
    </update>

    <delete id="delete" parameterType="MyRoute">
        delete from myroute
        where id=#{id}
    </delete>

    <select id="find" resultType="MyRoute">
        select * from myroute
        where id=#{id}
    </select>
</mapper>